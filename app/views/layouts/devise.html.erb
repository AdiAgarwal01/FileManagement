<!DOCTYPE html>
<html>
  <head>
    <title>File Manager</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Toastr CSS -->
    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>

    <!-- Toastr JavaScript -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/toastr.js/latest/js/toastr.min.js"></script>

    <!-- Toastr Options -->
    <style>
      body {
        font-family: Arial, sans-serif;
        background-color: #f8f9fa;
        margin: 0;
        padding: 0;
      }

      .card {
        width: 100%;  /* Full width */
        border-radius: 10px;
        margin: 20px 0;
        box-shadow: 0px 4px 8px rgba(0, 0, 0, 0.1);  /* Optional for a nice effect */
      }

      .container {
        margin-top: 50px;
        max-width: 800px;
        max-height: 500px;
      }

      .login-container {
        display: flex;
        height: 70vh;
      }

      .login-form {
        width: 50%;
        padding: 50px;
        background-color: #fff;
      }

      .welcome-section {
        width: 50%;
        background: linear-gradient(to right, #ff758c, #ff7eb3);
        color: white;
        display: flex;
        flex-direction: column;
        justify-content: center;
        align-items: center;
        text-align: center;
        padding: 50px;
      }

      h3, h2 {
        margin-bottom: 20px;
      }

      .form-group {
        margin-bottom: 15px;
      }

      .input-field {
        width: 100%;
        padding: 10px;
        border: 1px solid #ccc;
        border-radius: 5px;
      }

      .btn-submit {
        background: linear-gradient(to right, #ff758c, #ff7eb3);
        border: none;
        color: white;
        padding: 10px 20px;
        border-radius: 5px;
        cursor: pointer;
      }

      .btn-submit:hover {
        background: linear-gradient(to right, #ff6171, #ff6989);
      }

      .social-login {
        margin-top: 20px;
        text-align: center;
      }

      .icons a {
        margin: 0 10px;
        font-size: 24px;
        color: #ff7eb3;
      }

      .icons a:hover {
        color: #ff6171;
      }

      .forgot-password {
        float: right;
        color: #ff6171;
      }

      .btn-signup {
        margin-top: 20px;
        background-color: white;
        color: #ff7eb3;
        border: 1px solid white;
        padding: 10px 20px;
        border-radius: 5px;
        cursor: pointer;
        text-decoration: none;
      }

      .btn-signup:hover {
        background-color: #ff7eb3;
        color: white;
      }

    </style>
  </head>

  <body>
    <div class="container">
      <div class="row justify-content-center">
        <div class="card-body">
          <!-- Flash Messages -->
          <div class="content-container">
            <%= yield %>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
